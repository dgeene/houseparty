version: '3.8'

services:
  houseparty:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: houseparty-docs
    ports:
      - "${PORT:-8080}:80"
    restart: unless-stopped
    environment:
      - TZ=UTC
